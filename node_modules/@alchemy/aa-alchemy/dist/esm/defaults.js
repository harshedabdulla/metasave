import { arbitrum, arbitrumGoerli, arbitrumSepolia, optimism, optimismGoerli, optimismSepolia, } from "viem/chains";
export const getDefaultUserOperationFeeOptions = (chain) => {
    const feeOptions = {
        maxFeePerGas: { percentage: 50 },
        maxPriorityFeePerGas: { percentage: 5 },
    };
    if (new Set([
        arbitrum.id,
        arbitrumGoerli.id,
        arbitrumSepolia.id,
        optimism.id,
        optimismGoerli.id,
        optimismSepolia.id,
    ]).has(chain.id)) {
        feeOptions.preVerificationGas = { percentage: 5 };
    }
    return feeOptions;
};
//# sourceMappingURL=defaults.js.map