import { installPlugin } from "./installPlugin.js";
import { uninstallPlugin, } from "./uninstallPlugin.js";
export const pluginManagerDecorator = (provider) => ({
    installPlugin: (params, overrides) => installPlugin(provider, params, overrides),
    uninstallPlugin: (params, overrides) => uninstallPlugin(provider, params, overrides),
});
//# sourceMappingURL=decorator.js.map