import { encodePacked } from "viem";
export const encodeFunctionReference = (pluginAddress, functionId) => {
    return encodePacked(["address", "uint8"], [pluginAddress, Number(functionId)]);
};
//# sourceMappingURL=utils.js.map