import {} from "viem";
import { MultiOwnerPlugin, MultiOwnerPluginAbi } from "./plugin.js";
const ExtendedMultiOwnerPlugin_ = {
    ...MultiOwnerPlugin,
    accountMethods: (account) => {
        const og = MultiOwnerPlugin.accountMethods(account);
        return {
            ...og,
            readOwners: async (pluginAddress) => {
                const contract = MultiOwnerPlugin.getContract(account.rpcProvider, pluginAddress);
                return contract.read.ownersOf([await account.getAddress()]);
            },
        };
    },
};
export const ExtendedMultiOwnerPlugin = ExtendedMultiOwnerPlugin_;
//# sourceMappingURL=extension.js.map