import { arbitrum, arbitrumGoerli, arbitrumSepolia, base, baseGoerli, baseSepolia, sepolia, mainnet, optimism, optimismGoerli, optimismSepolia, polygon, polygonMumbai, } from "viem/chains";
export const getDefaultNaniAccountFactoryAddress = (chain) => {
    switch (chain.id) {
        case mainnet.id:
        case sepolia.id:
        case base.id:
        case baseGoerli.id:
        case baseSepolia.id:
        case polygon.id:
        case polygonMumbai.id:
        case arbitrum.id:
        case arbitrumGoerli.id:
        case arbitrumSepolia.id:
        case optimism.id:
        case optimismGoerli.id:
        case optimismSepolia.id:
            return "0x000000000000dD366cc2E4432bB998e41DFD47C7";
    }
    throw new Error(`no default nani account factory contract exists for ${chain.name}`);
};
//# sourceMappingURL=utils.js.map